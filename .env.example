DATABASE_URL="postgresql://postgres:postgres@localhost:5432/greenquote"
# Generate a fresh secret for any non-local environment, e.g.
#   openssl rand -hex 32
JWT_SECRET="dev-only-please-replace-with-openssl-rand-hex-32-output"
NODE_ENV="development"
